package com.string;

public class RemoveAndReplace {
    public static void main(String[] args) {

    }

    // write a program which takes as input an array of characters,
    // and removes each 'b' and replaces each 'a' by two 'd's
    private static String removeAndReplace(String input, char remove, char one, char two) {

        char[] charArray = input.toCharArray();
        for (int i = 0; i < charArray.length; i++) {

        }

        return "";
    }
}
